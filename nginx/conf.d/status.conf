# nginx-prometheus-exporter가 스크랩할 내부 전용 상태 엔드포인트.
# 8088은 호스트에 노출하지 않음(컨테이너 ports에 없음) — cicd-net 내부에서만 접근.
server {
    listen 8088;
    server_name _;

    location /stub_status {
        stub_status;
        access_log off;
    }
}
